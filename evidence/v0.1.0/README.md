# v0.1.0 Release Evidence

This directory is the sealed local release-candidate evidence package. It contains the automated verifier output, the demo report, and the API smoke-test facts. The demo intentionally returns INCONCLUSIVE because its synthetic sample is too small for a promotion claim.

