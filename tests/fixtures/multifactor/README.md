# Multi-factor compatibility fixture

These CSV files are synthetic contract fixtures for the public
`multi-factor-alpha-platform` weight and adjusted-close schemas. They are not
strategy artifacts and must not be used to support a performance or promotion
claim.
